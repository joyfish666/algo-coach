# USAGE

面向使用者的安装、启动与页面说明。文档随功能落地持续补充。

## 安装

要求 Python ≥ 3.10（Node.js 仅从源码构建前端时需要）：

```bash
git clone https://github.com/joyfish666/algo-coach.git
cd algo-coach
pip install -e .
cd web && npm install && npm run build && cd ..
```

发布版 wheel 将附带已构建前端，届时无需 Node。

## 启动

```bash
coach
```

- 服务仅绑定 `127.0.0.1`，默认端口 8000，被占用自动顺延。
- 横幅显示最终实际 URL；服务就绪后自动打开浏览器。
- 首次启动进入 `/setup` 引导页。
- 参数：`--port`（首选端口）、`--no-browser`（不自动开浏览器）、`--debug`（详细日志）。
- 配置优先级：CLI 参数 > 环境变量 > 配置文件。
- 数据目录：`~/.algocoach/`（POSIX 下权限收紧为 600；Windows 依赖用户目录保护）。

## /setup 向导

分步配置：粘贴 Cookie（后端即时校验有效性）→ LLM Key/URL（可跳过）→ 刷题语言与主题选择。
Cookie 失效后可在设置页更新，或清除后重走向导。

## 页面说明

| 路由 | 页面 | 说明 |
|---|---|---|
| `/problems` | 题库列表 | 「同步题库」拉取全量题库（3000+ 题，按限速约 2–3 分钟，请耐心等待）；难度/标签/关键词筛选 |
| `/problem/:qid` | 答题工作台 | 题面渲染 + CodeMirror 编辑器 + Run/Submit + 自定义用例面板 + AI 对话侧边栏 |
| `/daily` | 每日一题 | 今日题目卡片直达工作台 |
| `/analyze` | 分析仪表盘 | 解题统计、标签掌握度、AI 薄弱点报告 |
| `/settings` | 设置 | Cookie 管理、LLM 配置、默认语言、界面语言、主题切换 |

## 工作台要点

- **语言切换**：支持 cpp / python3 / java，未落盘的语言首次切换时需联网拉取模板；
  已落盘语言可断网复习。
- **Run 模式**：远程执行官方示例用例（需联网，本地不编译）；勾选「使用本地自定义用例」时
  使用「自定义用例」面板编辑的 testcases.txt。
- **Submit 模式**：正式提交进入站内提交历史，判定完成自动归档到本地 JSON Lines。
- **保存机制**：停止输入约 1–2s 自动写盘；异常退出后下次打开会提示恢复快照。

## 常见报错对照表

| 现象 | 含义与处理 |
|---|---|
| 403 / 提示 Cookie 过期 | Cookie 已失效，按引导重新粘贴 |
| 提示「打开题目并选择该语言」 | 该语言模板尚未落盘且当前离线，联网后打开题目并选择该语言即可 |
| 付费题标记 | Premium 题目无法拉取题面/判定，优雅跳过 |
| 429 / 触发限速 | 已自动退避；频繁出现请降低操作频率或调整请求间隔配置 |
| 「结果未知，请在站内确认」 | 判定轮询超时但提交已进入站内历史，归档已记录 submission_id 可追溯 |

## FAQ

- **数据存在哪里？** 全部在本地 `~/.algocoach/`，无云依赖。
- **界面语言和题面语言的关系？** 题面恒为中文（数据源为 leetcode.cn），界面语言仅影响 UI 文案。
