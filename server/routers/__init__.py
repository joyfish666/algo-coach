"""API routers grouped by concern.

Each module owns a slice of the REST surface and holds its request/response
models. Shared singletons, factories and lookup helpers come from
server.state; error shaping from server.errors.
"""
